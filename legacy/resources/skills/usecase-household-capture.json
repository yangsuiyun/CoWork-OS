{
  "id": "usecase-household-capture",
  "name": "Household Capture",
  "description": "Turn a messy household message into Notion tasks + optional reminders.",
  "icon": "🏠",
  "category": "Use Cases",
  "prompt": "Household capture workflow.\n\nGoal\n- Turn a messy message into clean tasks and log them to Notion.\n\nInputs\n- Notion database_id: {{notion_database_id}}\n- Raw tasks text:\n{{tasks}}\n- Create reminders: {{create_reminders}}\n\nSteps\n1) Parse the raw tasks text into a structured checklist. Infer:\n   - Priority (high/medium/low)\n   - Due date (if implied)\n   - Owner (if mentioned)\n2) If \"{{notion_database_id}}\" is missing or looks invalid, ask me for the database_id and STOP.\n3) Create one Notion page per task in that database using notion_action create_page.\n   - Put the task title in the Notion title property.\n   - If the database supports it, also set properties like due date, status, priority.\n4) If create_reminders is true:\n   - For tasks with due dates/times, create Apple Reminders via apple_reminders_action (or schedule_task if Reminders is unavailable).\n5) Return a concise report:\n   - Task list\n   - Notion page IDs/URLs\n   - Reminder IDs (if created)\n\nSafety\n- Ask for approval before creating or modifying external items if the tool requires it.\n",
  "parameters": [
    {
      "name": "notion_database_id",
      "type": "string",
      "description": "Target Notion database_id where tasks should be created",
      "required": true
    },
    {
      "name": "tasks",
      "type": "string",
      "description": "Paste the raw household tasks text here",
      "required": true
    },
    {
      "name": "create_reminders",
      "type": "boolean",
      "description": "Also create reminders for due tasks",
      "required": false,
      "default": true
    }
  ],
  "enabled": true,
  "metadata": {
    "routing": {
      "useWhen": "Use when the user asks to turn a messy household message into Notion tasks + optional reminders.",
      "dontUseWhen": "Do not use when the request is asking for planning documents, high-level strategy, or non-executable discussion; use the relevant planning or design workflow instead.",
      "outputs": "Outcome from Household Capture: task-specific result plus concrete action notes.",
      "successCriteria": "Returns concrete actions and decisions matching the requested task, with no fabricated tool-side behavior.",
      "examples": {
        "positive": [
          "Use the usecase-household-capture skill for this request.",
          "Help me with household capture.",
          "Use when the user asks to turn a messy household message into Notion tasks + optional reminders.",
          "Household Capture: provide an actionable result."
        ],
        "negative": [
          "Do not use when the request is asking for planning documents, high-level strategy, or non-executable discussion; use the relevant planning or design workflow instead.",
          "Do not use usecase-household-capture for unrelated requests.",
          "This request is outside household capture scope.",
          "This is conceptual discussion only; no tool workflow is needed."
        ]
      }
    },
    "authoring": {
      "complexity": "low"
    }
  }
}
